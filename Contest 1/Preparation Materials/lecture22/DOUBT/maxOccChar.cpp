//Give it a read 
// cin.getline(str, len);
//My Name is Khan
//Custom Deliited?

//int len = strlen(name);
// to compare s1 and s2
//strcmp(s1, s2);
    //!=0 when not equal
    // =0 when equal

//copy 
    //strcpy(dest, src);

//1:17:10 => change into the same string(inline changes)

//1:23:00 => how find, erase function of a string works internally?
